{
  "name": "easycookies",
  "version": "0.0.1",
  "description": "",
  "main": "easyCookies.js",
  "scripts": {
    "build": "rimraf dist && tsc && rollup -c",
    "test": "echo \"Error: no test specified\" && exit 1",
    "run": "lite-server -c lite-server-config.json"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lite-server": "^2.6.1",
    "rimraf": "^5.0.5"
  },
  "dependencies": {
  }
}
